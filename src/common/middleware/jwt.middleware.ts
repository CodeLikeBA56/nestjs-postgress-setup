/*
  Deprecated: JWT middleware moved to `JwtAuthGuard`.
  This file is retained as a stub to avoid accidental imports.
*/
export class JwtMiddleware {
  constructor() {
    throw new Error('JwtMiddleware removed — use JwtAuthGuard instead.');
  }
}
